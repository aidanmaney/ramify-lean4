{
  "name": "ramify-companion",
  "displayName": "Ramify",
  "description": "Lean proofs-as-trees visualizer and editor that lives in the infoview",
  "publisher": "aidan",
  "version": "0.0.13",
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "main": "./extension.js",
  "activationEvents": [
    "onStartupFinished",
    "onUri"
  ],
  "contributes": {
    "commands": [
      {
        "command": "ramify.openLens",
        "title": "Ramify: Open Tactic in Lens"
      }
    ],
    "configuration": {
      "title": "Ramify",
      "properties": {
        "ramify.outlineOnly": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Draw the tree's node boxes as borders with no fill, only showing the colored outline. Purely cosmetic."
        },
        "ramify.tallFrame": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Let the proof tree run closer to the bottom edge of the infoview. Stops a little short by default."
        },
        "ramify.linkMarks": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Draw the connectors' node-type marks: a dot near either end of a line for goals, a dash for tactics."
        },
        "ramify.linkTint": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Tint each connector based on the colour of the node it terminates at (blue for goals, green for tactics)."
        },
        "ramify.lensGoals": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Show each tactic's resulting goal inline after the tactic line. Drawn as decoration text."
        },
        "ramify.lensWordWrap": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Wrap long lines in the lens. Does not affect other editors or windows. Skipped when you already wrap globally."
        },
        "ramify.counterfactual": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "While you are typing a tactic and the proof doesn't elaborate (e.g. because it has errors), show the tree as if the line you are writing were `sorry` so the tree keeps its full shape, replaced with the real tree when the proof is fixed. Costs one re-elaboration of the current theorem per edited line (cached)."
        },
        "ramify.hypMarkStyle": {
          "type": "string",
          "enum": ["highlight", "underline"],
          "default": "highlight",
          "markdownDescription": "How the tree marks the hypotheses a tactic uses when you hover its box."
        },
        "ramify.typingHoldMs": {
          "type": "integer",
          "default": 600,
          "minimum": 0,
          "maximum": 5000,
          "markdownDescription": "How long (in milliseconds) the cursor must sit still after you type before the tree redraws to match the changed proof. Prevents redraw after every keystroke. Raise it if you type slowly and still see churn mid-edit; `0` redraws on every change. Edits made from the tree always redraw ASAP."
        }
      }
    }
  }
}
